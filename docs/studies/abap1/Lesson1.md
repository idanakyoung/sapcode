# 🧭 **Lesson 1 - ABAP OverView**

## 🔵 Unit1.  Flow of an ABAP Program

1. SAP 아키텍처
    1. SAP 아키텍처 전체 구조
    
    ```jsx
    ┌──────────────────────────────────────────────┐
    │                Presentation Layer            │ -> client 로도 불림
    │   SAP GUI / Browser / Fiori / Mobile         │
    └──────────────────────────────────────────────┘
    │
    ▼
    ┌──────────────────────────────────────────────┐
    │              Application Layer (AS)          │
    │                                              │
    │  AS ABAP                                     │
    │   ├── Dispatcher                             │
    │   ├── Work Processes (DIA, BTC, UPD, ENQ, SPO)│
    │   ├── Enqueue Server (Lock Manager)          │
    │   ├── ABAP Runtime System                     │
    │   │     ├── ABAP Kernel                      │
    │   │     ├── Interpreter / ABAP VM            │
    │   │     ├── Buffer System (Program/Table/etc)│
    │   │     ├── Memory Areas (Roll/Ext/Private)  │
    │   │     ├── Database Interface (DBI)         │
    │   │     ├── Reuse Library (REUSE Units)      │
    │   │     └── Screen Runtime (PBO/PAI)         │
    │                                              │
    │  AS Java                                     │
    │   └── J2EE Engine                             │
    │                                              │
    │  Message Server                               │
    │  Gateway (RFC/CPIC/ICM Communication)         │
    └──────────────────────────────────────────────┘
    │
    ▼
    ┌──────────────────────────────────────────────┐
    │                Database Layer                │ -> repository (저장소)
    │       HANA / Oracle / SQL Server / DB2       │
    └──────────────────────────────────────────────┘
    │
    ▼
    ┌──────────────────────────────────────────────┐
    │       Operating System & Hardware Layer      │
    │   Linux / Windows / Cloud / Storage / CPU     │
    └──────────────────────────────────────────────┘
    ```
    
2. AS ABAP란?
    1. **AS ABAP(Application Server ABAP)** 는 SAP 시스템의 핵심 애플리케이션 서버로, ABAP 코드가 실행되고, 트랜잭션이 처리되며, 사용자 요청을 관리하는 서버 계층을 말한다.
    
3. AS ABAP의 세부 구조 (중요)
    
    ① **Dispatcher :** 사용자의 요청을 Work Process로 배분
    
    ② **Work Process :** ABAP 코드 실행 엔진
    
    - DIA (Dialog)
    - BTC (Background)
    - UPD / UP2 (Update)
    - ENQ (Enqueue)
    - SPO (Spool)
    
    ③ **Message Server :** 여러 AS Instance 간 로드밸런싱
    
    ④ **Gateway :** RFC, CPIC → 외부 시스템과 통신
    
4. 워크 프로세스(Work Process, WP)
    
     1. DIA (Dialog WP)
    
    - 사용자 요청(대화형 작업) 처리
    - 짧고 빠른 트랜잭션 중심
    
     2. BTC (Background WP)
    
    - 배치 작업, 대량 데이터 처리
    - 사용자 세션 없이 실행됨
    
     3. UPD / UP2 (Update WP)
    
    - 데이터베이스 변경 작업(UPDATE/INSERT/DELETE) 처리
    - UP2는 비동기 업데이트 작업 처리
    
     4. SPO (Spool WP)
    
    - 출력 작업(프린트/스풀) 처리
    
     5. ENQ (Enqueue WP)
    
    - 락(lock) 관리
    - 테이블/레코드의 동시 접근을 제어 (SAP Lock Manager)
    1. Work Process 상태
        
        ![image.png](attachment:6301a644-a299-4f5a-87fa-36415cb874bb:image.png)
        
    
5. AS ABAP 처리 흐름(요청 → 응답)
    1. 사용자가 GUI/Web에서 요청 전송
    2. **Dispatcher** 가 요청을 수신
    3. 적절한 **Work Process(DIA/BTC)** 에 작업 배정
    4. 필요한 경우 **ENQ** 를 통해 Lock 제어
    5. DB 작업은 **UPD / UP2** 가 처리
    6. 필요한 출력은 **SPO** 가 처리
    7. 결과가 Dispatcher → 사용자에게 전달

---

## 🔵 Unit2.  ABAP Workbench

### A.  **SE84 - Repository**

1. **ABAP Repository(SE84)란?**

![image.png](attachment:56e631b6-b4f3-48bd-8646-a8ca97dec4d8:image.png)

= SAP 시스템에서 **ABAP 개발 객체 전체를 저장·관리하는 중앙 저장소**

- 프로그램, 클래스, 함수, DDIC 객체, UI 객체, Enhancement 등
- 모든 개발 결과물(Repository Objects)이 여기에 포함됨
- SE84/SE80을 통해 탐색 가능

---

2. **Repository의 두 가지 주요 분류(Filtering) 기준**

① **Package (기술적 분류 – Technical Layer)**

- 개발 객체가 *물리적으로 저장되는 기술적 단위*
- Transport Layer 및 개발 경로(Workbench/Customizing) 결정
- 예: BC400, ZPKG_FIN, SFLIGHT
- **Package = 개발 객체의 “기술적 집합체”**

② **Application Component (업무적 분류 – Functional Layer)**

- 개발 객체를 **업무 모듈 기준(SD/MM/FI/HR 등)** 으로 분류
- “이 객체가 어떤 SAP 기능 범주에 속하는가?”에 대한 답
- 예: SD-SLS, FI-GL, MM-PUR
- **Application Component = 개발 객체의 “업무적 역할 분류”**

---

3. **Repository 전체 구조 개념**

```json
ABAP Repository (전체 개발 객체 저장소)
│
├── Repository Objects (개발 결과물)
│     ├─ ABAP Dictionary (Tables, Views, Domain…)
│     ├─ Programs / Includes
│     ├─ Function Modules / Groups
│     ├─ Classes / Interfaces
│     ├─ Web Dynpro / BSP
│     ├─ Enhancements (BAdI, Enhancement Point)
│     └─ 기타 기술 객체 (Message Class, Test Objects 등)
│
├── Package (기술적 분류)
│     └─ 개발 객체를 저장하는 기술적 단위
│
└── Application Component (업무적 분류)
      └─ SAP 모듈/기능 기반 분류 구조

```

---

### B. SE80 – Object Navigator

1. Object Navigato : SAP의 통합 개발 환경(IDE). ABAP 개발 관련 모든 객체를 한 곳에서 조회·생성·관리하는 도구
2. SE80 주요 기능
    
    ① **개발 객체 전체 관리(Repository Navigator)**
    
    - 프로그램
    - 패키지
    - 클래스/인터페이스
    - 함수 그룹
    - 폼/스크린(Dynpro)
    - Web Dynpro
    - BSP
    - MIME Objects
    - Enhancements
    
    ② **ABAP Editor 통합 -** SE80 안에서 바로 코드 작성
    
    - 프로그램 코드 (SE38 기능 포함)
    - 클래스 메서드 코드 (SE24 기능 포함)
    - FM 로직 (SE37 기능 포함)
    
    ③ **Screen Painter (스크린 에디터) -** Dynpro UI 화면(PBO/PAI)을 드래그 방식으로 편집
    
    ④ **Menu Painter -** PF-Status, GUI 메뉴, Function Code 편집
    
    ⑤ **Object Creation Wizard -** 새 프로그램/클래스/테이블 등 생성할 때 템플릿 제공
    
    ⑥ **Package 단위 개발 관리 -** SE80은 기본 단위가 **Package**
    
    → 패키지 안의 모든 개발 객체 한눈에 보기
    
3. 핵심 T-Code 5종 요약

| T-Code | 이름 | 주요 목적 | 개발 대상 |
| --- | --- | --- | --- |
| **SE80** | Object Navigator | 전체 개발환경(IDE) | 모든 개발 객체 |
| **SE11** | ABAP Dictionary | 데이터 구조 정의 | 테이블, 구조, 도메인 등 |
| **SE37** | Function Builder | 함수 개발/테스트 | Function Module |
| **SE24** | Class Builder | 객체지향 개발 | 클래스/인터페이스 |
| **SE38** | ABAP Editor | 레포트/코드 개발 | ABAP 프로그램 |
1. Object Navigator Context Menu = 우클릭(Context Menu)
    1. 핵심 기능
        1. 생성(Create)
        2. 변경(Change)
        3. 활성화(Activate)
        4. 테스트(Test/Execute)
        5. Where-used

---

## 🔵 Unit3.  Transport Request & Task & Customer Namespace & Transport Layer

1.  Transport Request (요청, Request Number)

=  SAP 개발 변경을 **시스템 간(DEV → QA → PRD)** 이동시키기 위한 **최상위 단위의 전송 컨테이너**.

- 개발자들이 만든 객체를 담아두는 “큰 상자”
- 번호 형식 예: **DEVK900123**
- Request 안에 여러 Task가 포함됨
- QA/PROD로 릴리즈되는 단위
- **“시스템 이동을 위한 전송의 본체(컨테이너)”**

**2.  Transport Task (작업, Task Number)**

= 한 명의 개발자가 특정 Request 안에서 수행하는 **개별 작업 단위**.

- Request 아래에 딸린 하위 단위
- 번호 형식 예: **DEVK900123 → DEVK900124**
- 개발자가 실제로 작업할 때 자동 생성됨
- 작업 완료 후 Task를 Release하면 Request에 합쳐짐
- **“Request 안의 개발자 작업 단위(하위 컨테이너)”**

1. Customer Namespace (Y / Z 네임스페이스 작명 규칙)
    1. SAP 표준 객체와 구분하기 위해 **고객이 만든 모든 개발 객체는 반드시 Y 또는 Z로 시작해야 한다.**
    2. ABAP Repository에 속하는 거의 모든 개발 객체가 Z 또는 Y 네임스페이스를 사용한다.
    3. Y vs Z 차이
        
        
        | Prefix | 의미 |
        | --- | --- |
        | **Z** | 가장 일반적인 Customer Object prefix |
        | **Y** | 고객 전용 네임스페이스가 정의된 경우 사용 (주로 글로벌 기업) |
    4. ‘Prefix + 모듈 + 기능 + 번호 ‘형식으로 작명
    
2. Transport Layer (전송 레이어)
    1. 개발 객체가 어떤 경로(DEV → QA → PRD)로 이동할지 결정하는 전송 경로 규칙 즉 **어느 시스템 Landscape로 전송될지” 정해주는 레이어**
    2. **Transport Layer 역할**
        1. **Request가 어떤 시스템으로 이동할지 결정**
        2. 개발 객체가 **자동으로 올바른 시스템 순서로 이동되도록** 관리
        3. 패키지(Package)에 설정되어 모든 객체가 동일한 경로를 따름
3. sap gui 레이아웃
    1. 레이아웃
        
        ![image.png](attachment:951c1bae-4560-4e8a-94e7-7652d918ccc6:image.png)
        

---

## 🔵 Unit4.  실습1

### **1. Change Request(Transport Request) 생성**

- 워크벤치 선택
    
    ![image.png](attachment:89a07d12-dec1-4775-a55d-15e131695cba:image.png)
    
- 설명 지정 및 tasks : 팀 유저들 입력
    
    ![image.png](attachment:47819817-5df0-490f-8106-41c1e5011fc7:image.png)
    
- 생성 완료
    
    ![image.png](attachment:95efd8ec-21e5-4eb7-9c32-da4fbcc5fc73:image.png)
    

### 2. Package ( 패키지: 개발 객체의 저장 위치 + 관리 단위 + 전송 경로(Transport Layer) ) 생성

- 패키지 설정
    
    ![image.png](attachment:aa2c606e-d208-44b2-8e41-c84691e9898b:image.png)
    
- 생성 완료
    
    ![image.png](attachment:a80f174e-19cd-4183-b4bc-170f94b8e4a0:image.png)
    

---

## 🔵 Unit4.  실습2

### 1. 셀렉션 스크린 띄우기

- Selecction Screen : 1000 (스크린 번호)
    
    ![image.png](attachment:0bdc9714-5364-49e9-b88a-8c04fd7eb8d4:image.png)
    

---

## 🔵 Unit5. ABAP 기본 문법 & 주석 정리

### 1. ABAP 문장 기본 구조

| 구성 요소 | 설명 | 예시 |
| --- | --- | --- |
| ABAP Keyword | 문장의 첫 단어, 명령어 | `DATA`, `WRITE`, `IF`, `SELECT` |
| Additions & Operands | 키워드 관련 파라미터/피연산자 | `TYPE i`, `TO gv_result` |
| Period (.) | 문장 종료 표시 | `DATA lv_num TYPE i.` |

---

### 2. 주요 ABAP 기본 키워드

| 키워드 | 의미 / 역할 | 예시 |
| --- | --- | --- |
| `PARAMETERS` | 사용자 입력 필드 선언 | `PARAMETERS pa_num TYPE i.` |
| `DATA` | 변수 선언 | `DATA gv_total TYPE i.` |
| `MOVE` | 값 복사 (옛 문법) | `MOVE pa_num TO gv_result.` |
| `=` | 값 대입 (신 문법) | `gv_result = pa_num.` |
| `ADD` | 덧셈 | `ADD 1 TO gv_result.` |
| `WRITE` | 화면 출력 | `WRITE gv_result.` |
| `NEW-LINE` | 줄바꿈 | `NEW-LINE.` |

---

### 3. ABAP 주석(Comment)

| 유형 | 작성 방법 | 예시 | 특징 |
| --- | --- | --- | --- |
| **라인 주석** | `"` 로 시작 | `WRITE gv_num. "출력` | 라인의 나머지 전체 주석 처리 |
| **블록 주석** | `/* ... */` | `/* 여러 줄 주석 */` | ADT(Eclipse) 권장, 구버전 GUI는 제한 |

---

### 4. ABAP 코드 스타일 & 네이밍 규칙

| 규칙 | 설명 | 예시 |
| --- | --- | --- |
| Prefix 기반 변수명 | SAP 일반 규칙(가독성) | `lv_`, `gv_`, `lt_`, `ls_` |
| PARAMETERS prefix | 사용자 입력 | `pa_` |
| 들여쓰기 | IF/LOOP/CASE block에서 필수 | `IF lv_num > 0.` |
| 신문법 사용 | 문자열, 대입문 개선 | `WRITE |

---

### 5. 예제 프로그램 구성 (해설 포함)

| 코드 | 설명 | 특이사항 |
| --- | --- | --- |
| `PARAMETERS pa_num TYPE i.` | 사용자 입력 선언 |  |
| `DATA gv_result TYPE i.` | 변수 선언 | 변수 이름 시작은 g(global) , l (local) 두 번째 단어는 v,s,t 중 하나 |
| `MOVE pa_num TO gv_result.` | 값 복사 |  |
| `ADD 1 TO gv_result.` | 계산 (pa_num + 1) |  |
| `WRITE 'Your input:'.` | 텍스트 출력 |  |
| `WRITE pa_num.` | 입력값 출력 |  |
| `NEW-LINE.` | 줄바꿈 |  |
| `WRITE 'Result:'.` | 결과 제목 출력 |  |
| `WRITE gv_result.` | 계산 결과 출력 |  |

### 6.  T- Code  생성

- /ose93
    
    ![image.png](attachment:58ad01ce-d778-4c14-bb5b-29635e78ba8b:image.png)
    
- 프로그램 개별 우클릭 → create → Transaction
    
    ![image.png](attachment:30a7b95b-a577-401e-a3c1-c29319750bfd:image.png)
    
    ![image.png](attachment:923b1add-ccb9-4c61-a7bc-275f58a11e78:image.png)
    
    ![image.png](attachment:deb9fc05-c350-4c29-92ea-9178c00de857:image.png)
    

---

## 🔵 Unit6. 기본 ABAP 실습  코드

![image.png](attachment:163ad544-7b1b-49c6-bd66-d2f836c915df:image.png)

### #1 - exercise 1

```json
*&---------------------------------------------------------------------*
*& Report ZABAP_01_G01
*&---------------------------------------------------------------------*
*&
*&---------------------------------------------------------------------*
REPORT ZABAP_01_G01.
* selection Screen 번호 1000 생성 및 Input Field 생성.
* 정수 값만 입력 가능.
* 화면의 입력 필드에 입력한 값은 pa_num에 할당(pa_num 변수에 해당).

PARAMETERS PA_NUM TYPE I.
* 변수 선언.
* 변수에 정수 값만 할당 가능.
DATA GV_RESULT TYPE I. "gv_result는 정수값을 받음".

* 화면에 입력 필드에 입력된 값을 변수(gv_result)에 할당.
MOVE PA_NUM TO GV_RESULT.

* 선언된 변수에 값을 더하기.
* gv_result 변수에 1이 더함.
ADD 1 TO GV_RESULT. ADD 2 TO GV_RESULT. "여러 문장이 한 라인에 올 수 있음.

* 화면에 Your Input : 문자열과 화면에 입력한 값(pa_nu)을 같이 표시.
"WRITE 'Your Input : '.RITE 'Your Input : '.
"WRITE PA_NUM.
WRITE: 'Your Input :' , PA_NUM. "chain statement.

* 다음 줄(새로운 라인).
NEW-LINE.

* 화면에 Result : 문자열과 변수에 할당된 값(gv_result)을 같이 표시.
"WRITE 'Result : '.
"WRITE GV_RESULT.
WRITE : 'Result : ',
      GV_RESULT.
```

### #2 - exercise 2

```json
*&---------------------------------------------------------------------*
*& Report ZBC400_G01_HELLO
*&---------------------------------------------------------------------*
*&
*&---------------------------------------------------------------------*
REPORT ZBC400_G01_HELLO.

* Selection screen - 번호 1000, Input field 생성.
* data type string (문자열)
PARAMETERS : pa_name TYPE string.

* Hello World 텍스트 출력.
WRITE 'Hello World!'.

* 다음 줄 (줄바꿈).
NEW-LINE.

* 화면에 입력된 이름 앞에 Hello 텍스트 같이 표시.
*WRITE : 'Hello'.
*WRITE :  pa_name.
WRITE : 'Hello',
         pa_name.
```
